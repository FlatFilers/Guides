### November 26, 2024
## spaces

spaces-ui@deb457c
The nativeActions object has been updated with the following changes: the "removeColumn" key now has the correct value "Remove Column", and the "addColumn" key has been corrected to have the value "Add Column" instead of the previously incorrect "Remove column".