### August 14, 2026

## spaces

Fixes a bug where `space:configure` could silently provision fewer Workbooks than configured when several were created in parallel, leaving a Space with missing Workbooks and no clear signal.
