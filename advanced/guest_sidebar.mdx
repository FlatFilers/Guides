---
title: Guest Sidebar
description: "Learn how to limit what Guests see in their Sidebar."
---

<Note>This configuration only impacts Guests, not Admins. [Learn more about user access](hello.com).</Note>

Flatfile allows you to update your sidebar to hide/show certain elements via the <Tooltip tip="Learn more about Spaces">[Space](hello.com)</Tooltip> component. 

<RequestExample>
```ts  spaces.update 
const updateSpace = await flatfile.spaces.update(spaceId, {
    environmentId: environmentId,
    metadata: {
      sidebarConfig: {
        //See reference for all possible variables
      }
    }
  });
```
</RequestExample>

![Sidebar Config](../images/customize/sidebarConfig.png)

| Prop                  | Required | Type | Description |
| --------------------- | ------------   | ------------   | ------------   |
| `showGuestInvite`       |      `optional` | **boolean** | The Collaborators link in the sidebar allows anyone to add another Guest to the Space.     |
| `showDataChecklist`     |      `optional` | **boolean** | The data checklist shows all of the data needed for uploading to the primary Workbook.    |
| `showSidebar`           |      `optional` | **boolean** | The sidebar as a whole can be hidden.     |

