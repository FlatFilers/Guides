---
title: "Create a Workbook"
description: "Make the API call to create a Workbook from our docs, or copy and run the provided cURL"
api: "POST https://platform.flatfile.com/api/v1/workbooks"
---

<Tip>
  The Bearer token you will need for this call is your unique [secret
  key](../developer-tools/security/authentication#secret-and-publishable-keys).
  It can be found in the Developer settings of your Flatfile account [here
  ](https://platform.flatfile.com/developers).
</Tip>

### Body

<ParamField
  body="name"
  type="string"
  required
  initialValue="My First Workbook"
  default="My First Workbook"
  placeholder="My First Workbook"
>
  The name of the Workbook
</ParamField>

<ParamField body="sheets" type="array" required>
  <Expandable title="Sheet">
    <ParamField
      body="name"
      type="string"
      required
      initialValue="Contacts"
      default="Contacts"
      placeholder="Contacts"
    >
      The name of the Sheet
    </ParamField>
    <ParamField
      body="slug"
      type="string"
      required
      initialValue="contacts"
      default="contacts"
      placeholder="contacts"
    >
      The slug of the Sheet
    </ParamField>
    <ParamField body="fields" type="array" required>
      <Expandable title="Field">
        <ParamField
          body="key"
          type="string"
          required
          initialValue="firstName"
          default="firstName"
          placeholder="firstName"
        >
          The field key
        </ParamField>
        <ParamField
          body="type"
          type="string"
          required
          initialValue="string"
          default="string"
          placeholder="string"
        >
          The field type
        </ParamField>
        <ParamField
          body="label"
          type="string"
          required
          initialValue="First Name"
          default="First Name"
          placeholder="First Name"
        >
          The field label
        </ParamField>
      </Expandable>
    </ParamField>

  </Expandable>
</ParamField>

<ParamField body="actions" type="array" required>
  <Expandable title="Action">

{" "}

<ParamField
  body="operation"
  type="string"
  initialValue="submitActionFg"
  default="submitActionFg"
  placeholder="submitActionFg"
>
  The name of the job operation
</ParamField>

     <ParamField
      body="mode"
      required
      type="string"
      initialValue="foreground"
      default="foreground"
      placeholder="foreground"
    >
      foreground / background - informs if the job blocks the UI while
      processing
    </ParamField>

    <ParamField
      body="label"
      required
      type="string"
      initialValue="Submit"
      default="Submit"
      placeholder="Submit"
    >
      The text on the button
    </ParamField>

    <ParamField
      body="description"
      type="string"
      initialValue="Submit data to webhook.site"
      default="Submit data to webhook.site"
      placeholder="Submit data to webhook.site"
    >
      The text that shows up in dialog when action is clicked.
    </ParamField>

    <ParamField
      body="primary"
      type="boolean"
      initialValue="true"
      default="true"
      placeholder="true"
    >
      Decides if the button will appear prominently.
    </ParamField>

  </Expandable>
</ParamField>

### Request headers

<ParamField
  header="Accept"
  required
  default="application/json"
  placeholder="application/json"
>
  Request header
</ParamField>

<ParamField
  header="Content-type"
  required
  default="application/json"
  placeholder="application/json"
>
  Request header
</ParamField>

<RequestExample>
```bash
curl --request POST \
  --url https://platform.flatfile.com/api/v1/workbooks \
  --header 'Authorization: Bearer PASTE_SECRET_KEY_HERE' \
  --header 'Content-Type: application/json' \
  --data '{
  "name": "My First Workbook",
  "labels": ["simple-demo"],
  "sheets": [
    {
      "name": "Contacts",
      "slug": "contacts",
      "fields": [
        {
          "key": "firstName",
          "type": "string",
          "label": "First Name"
        },
        {
          "key": "lastName",
          "type": "string",
          "label": "Last Name"
        },
        {
          "key": "email",
          "type": "string",
          "label": "Email"
        }
      ]
    }
  ],
  "actions": [
    {
      "operation": "submitActionFg",
      "mode": "foreground",
      "label": "Submit",
      "description": "Submit data to webhook.site",
      "primary": true
    }
  ]
}'
```
</RequestExample>

---

## Next steps

Now that you've created a Workbook, check it out in your [Dashboard](https://platform.flatfile.com). Next, we will author code to listen and react to Events as they occur.
